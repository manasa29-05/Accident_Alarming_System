# Accident Alarming System (Python)

This project detects accidents using sensor readings and sends an alert.

## How it works
- `sensor_reader.py` → reads or simulates accelerometer data  
- `detector.py` → checks if values exceed a threshold  
- `notifier.py` → sends alert message  
- `main.py` → main loop that runs everything together
